# 强化学习资产配置_任务单

## 里程碑 M1: 正式平台骨架与配置入口可运行
- **状态**: 部分完成
- **阶段性成果**: 仓库具备 `src/`、`configs/`、`tests/`、`requirements.txt`、`pyproject.toml` 的正式平台骨架，配置可被严格加载，CLI 可创建合法 run 目录但不触碰 `scripts/`、`vendor/`、`data/` 既有契约。
- **完成判定**: `.venv/bin/python -m pytest tests/test_config.py tests/test_cli_entrypoint.py tests/test_device.py` 全部通过；`ConfigLoader.load` 能生成稳定 `config_hash`；非法配置、非法路径、不可用设备均 fail fast。
- **任务清单**:
  - [ ] M1-T1 建立正式 Python 包、依赖与 pytest 入口
    - **目标**: 新增 `src/__init__.py`、`requirements.txt`、`pyproject.toml`，固定 pytest `testpaths = ["tests"]` 与 `pythonpath = ["."]`，并只声明正式平台依赖。
    - **涉及**: `src/__init__.py`、`requirements.txt`、`pyproject.toml`
    - **完成标准**: `requirements_data_pipeline.txt` 未被修改；`requirements.txt` 包含 `torch`、`gymnasium`、`scikit-learn`、`PyYAML`、`pandas`、`numpy`、`pyarrow`、`scipy`、`statsmodels`、`matplotlib`、`seaborn`、`optuna`、`pytest`；`quantstats` 不在核心依赖中。
    - **验证方式**: `.venv/bin/python -m pytest --collect-only`
  - [ ] M1-T2 定义配置默认值与严格 schema
    - **目标**: 在 `src/config.py` 实现 `DEFAULT_CONFIG`、`ConfigLoader.load`、`ConfigError`，覆盖详细设计第 3.1 节所有顶层 key 与嵌套默认值。
    - **涉及**: `src/config.py`、`tests/test_config.py`
    - **完成标准**: 缺失 key 自动补齐；未知 top-level 或 nested key 抛出 `ERR_CONFIG_UNKNOWN_KEY` 且错误信息包含完整 key path。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_config.py::test_unknown_key_fails_fast tests/test_config.py::test_defaults_are_filled`
  - [ ] M1-T3 实现路径白名单与离线模式校验
    - **目标**: 在 `src/config.py::assert_path_allowed` 校验 config、data、output、checkpoint load、benchmark CSV 路径必须落入 `security.path_whitelist`。
    - **涉及**: `src/config.py`、`tests/test_config.py`
    - **完成标准**: 父目录、网络路径、白名单外绝对路径抛出 `ERR_SECURITY_PATH_DENIED`；默认 `security.offline_mode=true`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_config.py::test_path_whitelist_denies_parent_path`
  - [ ] M1-T4 实现执行模型配置校验
    - **目标**: 在 `ConfigLoader.validate_execution_model` 校验 `next_close` 必须搭配 `delayed_action_execution=true`，同收盘价理想成交必须派生或校验 `idealized_execution=true`。
    - **涉及**: `src/config.py`、`tests/test_config.py`
    - **完成标准**: `execution_price="next_close"` 且 `delayed_action_execution=false` 抛出 `ERR_CONFIG_INVALID_EXECUTION_MODEL`；主配置默认 `execution_price="next_open"`、`same_close_idealized_execution_enabled=false`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_config.py::test_next_close_requires_delayed_execution`
  - [ ] M1-T5 实现组合资金规模与成本配置校验
    - **目标**: 在 `ConfigLoader` 校验 `cost_model.market_impact_enabled=true` 时 `portfolio.initial_capital_currency > 0`，并校验 `fixed_cost_unit`、`cost_model.mode` 枚举。
    - **涉及**: `src/config.py`、`tests/test_config.py`
    - **完成标准**: 缺少真实资金规模抛出 `ERR_CONFIG_PORTFOLIO_VALUE_REQUIRED`；非法成本模式抛出 `ERR_CONFIG_INVALID_COST_MODE`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_config.py::test_market_impact_requires_capital`
  - [ ] M1-T6 实现稳定配置哈希与快照序列化
    - **目标**: 在 `src/config.py` 实现 `canonical_json` 与 `config_hash`，在 `src/utils/logger.py` 提供 `save_yaml_atomic`。
    - **涉及**: `src/config.py`、`src/utils/logger.py`、`tests/test_config.py`
    - **完成标准**: 同一配置在 key 顺序不同的 YAML 中得到相同 64 位 sha256 hex；写文件先临时文件再原子替换。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_config.py::test_config_hash_is_stable`
  - [ ] M1-T7 实现设备选择与 AMP 开关
    - **目标**: 在 `src/utils/device.py` 实现 `get_device`、`move_batch_to_device`、`enable_amp_if_available`。
    - **涉及**: `src/utils/device.py`、`tests/test_device.py`
    - **完成标准**: `device.mode=auto` 按 CUDA、MPS、CPU 顺序选择；强制不可用设备抛出 `ERR_DEVICE_UNAVAILABLE`；CPU/MPS 不启用 AMP。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_device.py`
  - [ ] M1-T8 实现单实验 CLI 最小闭环
    - **目标**: 在 `src/experiments/run_experiment.py::main` 解析 `--config`、`--experiment` alias、`--seed`、`--device`、`--output`、`--run-name`，完成配置加载、路径校验、seed 覆盖和 run 目录创建。
    - **涉及**: `src/experiments/run_experiment.py`、`src/utils/seed.py`、`tests/test_cli_entrypoint.py`
    - **完成标准**: 合法命令创建 `results/<run_name>/logs/config_snapshot.yaml`；`--experiment configs/experiments/main.yaml` 与 `--config configs/experiments/main.yaml` 行为等价；非法 `run_name` 抛出 `ERR_OUTPUT_INVALID_RUN_NAME`；不会写入 `data/processed/`、`data/metrics_factory/`、`data/reports/`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_cli_entrypoint.py::test_run_experiment_creates_config_snapshot tests/test_cli_entrypoint.py::test_experiment_alias_matches_config`
  - [ ] M1-T9 创建基础配置文件
    - **目标**: 新增 `configs/main_model.yaml`、`configs/baselines.yaml`、`configs/ablation.yaml`、`configs/rewards.yaml` 与 `configs/experiments/*.yaml`，字段可被 `ConfigLoader` 全量加载。
    - **涉及**: `configs/`、`tests/test_config.py`
    - **完成标准**: 所有 YAML 均通过 schema 校验；实验类型覆盖需求枚举；默认 `security.path_whitelist` 指向 `/Users/chenjunming/Desktop/DRL_PM`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_config.py::test_all_config_files_load`

## 里程碑 M2: 离线数据、特征矩阵与泄漏控制可集成
- **状态**: 部分完成
- **阶段性成果**: 平台能从本地 `data/processed/` 与 `data/metrics_factory/` 加载标准资产池，封装 panel 到 wide、收益、amount proxy 和基础 schema 预处理，提供显式数据再生成入口，构造可用性掩码、审计 MetricsFactory 全量特征、生成市场图像、执行 train-only 预处理与 split 泄漏检查。
- **完成判定**: `.venv/bin/python -m pytest tests/test_data_loader.py tests/test_feature_matrix.py tests/test_feature_reduction.py tests/test_splits.py tests/test_leakage_checks.py tests/test_auxiliary_targets.py` 全部通过；不得联网补数；不得硬编码旧行数与旧日期范围。
- **任务清单**:
  - [ ] M2-T1 定义数据契约异常与 `MarketDatasetBundle`
    - **目标**: 在 `src/data/loader.py` 定义 `DataContractError`、`MarketDatasetBundle` 与 required path 解析。
    - **涉及**: `src/data/loader.py`、`tests/test_data_loader.py`
    - **完成标准**: `MarketDatasetBundle` 字段包含 `asset_universe`、`panel`、`wide`、`metrics_features`、`feature_cols`、`auxiliary_target_cols`、`availability_mask`、`availability_reason`、`data_manifest`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py::test_market_dataset_bundle_contract`
  - [ ] M2-T2 实现 panel 到 wide 预处理封装
    - **目标**: 在 `src/data/preprocess.py` 实现 `validate_panel_schema`、`panel_to_wide_tables`、`build_return_fields`、`detect_amount_proxy`，供数据加载和显式再生成入口复用。
    - **涉及**: `src/data/preprocess.py`、`tests/test_data_loader.py`
    - **完成标准**: 能从 panel 生成 open/high/low/close/pre_close/pct_chg/log_return/amount/vol/turnover_rate wide 表；基础字段缺失抛出 `ERR_DATA_SCHEMA_MISMATCH`；`amount=close*vol*100` 代理口径返回 `amount_is_proxy=true`；不执行联网补数。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py::test_preprocess_panel_to_wide_and_proxy_flags`
  - [ ] M2-T3 建立显式 AkShare ETF 数据再生成入口
    - **目标**: 新增 `src/data/akshare_etf_fetcher.py::main` 作为显式再生成命令入口，调用预处理封装并写入新的 `data_manifest`。
    - **涉及**: `src/data/akshare_etf_fetcher.py`、`src/data/preprocess.py`、`src/data/loader.py`、`tests/test_data_loader.py`
    - **完成标准**: 文件存在且只能由显式命令触发；`load_market_dataset`、训练入口、评估入口、回测入口均不 import 或调用 `akshare_etf_fetcher`；再生成成功时写入新的 `data_manifest` 并记录生成时间、源范围、proxy 标志。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py::test_akshare_fetcher_is_explicit_only`
  - [ ] M2-T4 实现资产池加载与标准资产顺序
    - **目标**: 在 `load_market_dataset` 读取 `data/processed/asset_universe.csv`，过滤 `status == "ok"` 并生成 `canonical_asset_order`。
    - **涉及**: `src/data/loader.py`、`data/processed/asset_universe.csv`、`tests/test_data_loader.py`
    - **完成标准**: 字段缺失抛出 `ERR_DATA_SCHEMA_MISMATCH`；无可用资产抛出 `ERR_DATA_NO_AVAILABLE_ASSET`；不回退旧脚本 `CONFIG['fund_list']`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py::test_asset_universe_status_ok_order`
  - [ ] M2-T5 实现 wide parquet 读取与列顺序校验
    - **目标**: 读取 `wide_open/high/low/close/pre_close/pct_chg/log_return/amount/vol/turnover_rate.parquet` 并 reindex 到标准资产顺序。
    - **涉及**: `src/data/loader.py`、`data/processed/*.parquet`、`tests/test_data_loader.py`
    - **完成标准**: 日期索引严格递增；列无法对齐或缺资产抛出 `ERR_DATA_SCHEMA_MISMATCH`；缺文件抛出 `ERR_DATA_MISSING_FILE` 并列出所有缺失路径。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py::test_wide_tables_align_to_asset_order`
  - [ ] M2-T6 实现 panel 与 MetricsFactory 特征加载校验
    - **目标**: 校验 `etf_lof_daily_panel.parquet` 无重复 `(trade_date, ts_code)`，`all_metrics_features.parquet` 无重复 `(date, ts_code)`。
    - **涉及**: `src/data/loader.py`、`data/metrics_factory/all_metrics_features.parquet`、`tests/test_data_loader.py`
    - **完成标准**: `metrics_factory.enabled=false` 时允许 `metrics_features=None` 并写入 manifest 标志；默认启用时缺失文件 fail fast。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py::test_metrics_features_unique_keys`
  - [ ] M2-T7 构造 `availability_mask` 与 `availability_reason`
    - **目标**: 基于 `wide_close`、`wide_log_return`、`asset_universe.first_date/last_date` 构造 `availability_mask` 与互斥原因枚举。
    - **涉及**: `src/data/loader.py`、`tests/test_data_loader.py`
    - **完成标准**: 原因仅为 `listed`、`not_listed_yet`、`delisted`、`suspended`、`missing_quote`、`missing_return`；无法识别停牌时在 `data_manifest.availability_reason_inference="field_based"` 记录。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py::test_availability_reason_is_exclusive`
  - [ ] M2-T8 处理 turnover_rate 全空与 proxy amount 口径
    - **目标**: 在 loader 和特征审计中识别 `wide_turnover_rate` 全空、`amount=close*vol*100` 代理口径。
    - **涉及**: `src/data/loader.py`、`src/data/feature_matrix.py`、`tests/test_data_loader.py`
    - **完成标准**: 默认不因 turnover_rate 全空失败；`turnover_rate_required=true` 抛出 `ERR_DATA_TURNOVER_RATE_REQUIRED`；manifest 标记 `amount_is_proxy=true` 与 `turnover_rate_all_missing=true`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py::test_turnover_rate_all_missing_policy`
  - [ ] M2-T9 实现 fixed、walk-forward、purged、embargo split
    - **目标**: 在 `src/data/splits.py::create_split` 生成 `SplitSpec`，包含 `train_dates`、`validation_dates`、`test_dates`、`fold_id`。
    - **涉及**: `src/data/splits.py`、`tests/test_splits.py`
    - **完成标准**: fixed 默认 70/15/15；walk-forward 为 3 年 train、6 个月 validation、6 个月 test、6 个月 step；空 split 抛出 `ERR_SPLIT_EMPTY`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_splits.py`
  - [ ] M2-T10 实现泄漏检查工具
    - **目标**: 在 `src/data/leakage_checks.py` 实现 `assert_no_future_label_in_features`、`assert_pca_not_fit_on_validation_or_test`、`assert_rolling_estimator_uses_past_only`、`assert_no_execution_field_in_observation`。
    - **涉及**: `src/data/leakage_checks.py`、`tests/test_leakage_checks.py`
    - **完成标准**: future label、非 train fit、execution-only 字段进入 observation 时分别抛出冻结错误码。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_leakage_checks.py`
  - [ ] M2-T11 实现 G0/G1/G2 基础特征构造
    - **目标**: 在 `src/data/feature_matrix.py::FeatureMatrixBuilder` 构造 G0 价格收益、G1 OHLCV、G2 技术指标固定公式。
    - **涉及**: `src/data/feature_matrix.py`、`tests/test_feature_matrix.py`
    - **完成标准**: G2 窗口为 `[5,10,20,60,120]`；所有特征按 `date, ts_code` 输出；`feature_cols` 不包含 auxiliary target。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_feature_matrix.py::test_g2_feature_formulas`
  - [ ] M2-T12 实现 G3 MetricsFactory 全量审计
    - **目标**: 审计 `all_metrics_features.parquet` 所有非 `date`/`ts_code` 列，生成 pass/warning/fail/dropped provenance。
    - **涉及**: `src/data/feature_matrix.py`、`src/data/leakage_checks.py`、`tests/test_feature_matrix.py`
    - **完成标准**: 命中 `future|forward|label|target|next|lead|t+1` 的列不得入模；warning 按 `feature_audit.warning_policy` 处理；fail 和 dropped 也进入报告。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_feature_matrix.py::test_metrics_factory_full_audit_statuses`
  - [ ] M2-T13 实现 G4 cross-asset risk 特征
    - **目标**: 在 `FeatureMatrixBuilder` 构造 rolling beta/corr/cov、asset vol、benchmark regime proxy 与 60 日相关矩阵特征向量载荷。
    - **涉及**: `src/data/feature_matrix.py`、`tests/test_feature_matrix.py`
    - **完成标准**: 只使用 available assets；特征向量符号按元素和固定；输出 `trend_signal` 与 benchmark 风险代理。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_feature_matrix.py::test_g4_cross_asset_features_are_past_only`
  - [ ] M2-T14 输出 provenance 与 MetricsFactory 抽样复核
    - **目标**: 生成 `feature_provenance_report.csv`、`feature_group_summary.csv`、`metrics_factory_audit_sample.csv` 的固定 schema。
    - **涉及**: `src/data/feature_matrix.py`、`tests/test_feature_matrix.py`
    - **完成标准**: turnover_rate 全空特征标记 `leakage_check_status="dropped"`、`drop_reason="all_missing"`；抽样复核失败抛出 `ERR_METRICS_FACTORY_AUDIT_FAILED`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_feature_matrix.py::test_feature_provenance_schema tests/test_feature_matrix.py::test_metrics_factory_audit_sample`
  - [ ] M2-T15 实现 `MarketImageDataset` lazy 与 materialize 模式
    - **目标**: 在 `src/data/feature_matrix.py` 实现 `MarketImageDataset.__len__`、`__getitem__`、`materialize`，输出 `[n_features, window_size, n_assets]`。
    - **涉及**: `src/data/feature_matrix.py`、`tests/test_feature_matrix.py`
    - **完成标准**: `date_index` 只包含具备完整历史窗口的 decision dates；lazy 模式下 `market_images` 可为 None 但索引稳定；market image 不含 execution-only 字段。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_feature_matrix.py::test_lazy_market_image_index_alignment`
  - [ ] M2-T16 实现 train-only 特征降维流水线
    - **目标**: 在 `src/data/feature_reduction.py::FeatureReductionPipeline` 实现 train median imputer、winsorize、StandardScaler、selector、PCA。
    - **涉及**: `src/data/feature_reduction.py`、`tests/test_feature_reduction.py`
    - **完成标准**: `fit` 只接收 train split；validation/test 只能 `transform`；PCA 不处理 `availability_mask`、future labels、date、ts_code、portfolio state。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_feature_reduction.py::test_train_only_fit_scope`
  - [ ] M2-T17 实现 M0-M7 输入矩阵选择规则
    - **目标**: 按 `feature_matrix.input_matrix_id` 控制 G0-G4 及其组合、PCA、feature selection 的组合。
    - **涉及**: `src/data/feature_matrix.py`、`src/data/feature_reduction.py`、`tests/test_feature_reduction.py`
    - **完成标准**: M6 默认 G1+G2+G3+G4+PCA；M7 默认 G1+G2+G3+G4+feature selection+PCA 且先 selector 再 PCA；PCA 输出后重新拼接 availability mask 非 PCA channel。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_feature_reduction.py::test_input_matrix_m0_to_m7_composition`
  - [ ] M2-T18 实现辅助标签生成与边界 purge
    - **目标**: 在 `src/data/auxiliary_targets.py` 生成 future return、volatility、trend、rank、downside、max drawdown、CVaR、covariance、masked reconstruction 标签。
    - **涉及**: `src/data/auxiliary_targets.py`、`tests/test_auxiliary_targets.py`
    - **完成标准**: future target 跨越 train/validation/test 边界的样本被 purge；辅助标签不进入 `feature_cols`、observation 或 market image。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_auxiliary_targets.py`

## 里程碑 M3: 统一组合执行、环境与回测链路可运行
- **状态**: 部分完成
- **阶段性成果**: 平台拥有统一 `PortfolioExecutionCore`，BacktestEngine 与 Gymnasium 环境共用同一套成交、成本、约束、奖励、NAV 更新逻辑，并能在 next_open / next_close / same_close debug 口径下输出冻结 schema。
- **完成判定**: `.venv/bin/python -m pytest tests/test_execution_core.py tests/test_cost_model.py tests/test_constraint_manager.py tests/test_rebalance_scheduler.py tests/test_reward_calculator.py tests/test_env.py tests/test_backtest_engine.py tests/test_baselines.py::test_base_strategy_contract` 全部通过。
- **任务清单**:
  - [ ] M3-T1 定义组合状态与动作 dataclass
    - **目标**: 在 `src/envs/state.py` 定义 `PortfolioState`、`DecisionMarketState`、`ExecutionMarketState`、`ExecutionResult`、`PortfolioAction`、`PendingAction`。
    - **涉及**: `src/envs/state.py`、`tests/test_execution_core.py`
    - **完成标准**: 字段与详细设计第 3.2 节一致；日期字段必填；数组 shape 由构造校验。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_execution_core.py::test_state_dataclass_contracts`
  - [ ] M3-T2 实现 `CostModel.estimate` 默认成本公式
    - **目标**: 在 `src/envs/cost_model.py` 实现 proportional、fixed、slippage、market impact 四类成本。
    - **涉及**: `src/envs/cost_model.py`、`tests/test_cost_model.py`
    - **完成标准**: market impact 使用 `portfolio_state.portfolio_value`、`trade_weight_i`、`ADV20_i`、`sigma20_i`；不得使用 `turnover^2 / liquidity_proxy`；turnover=0 返回全 0。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_cost_model.py::test_empirical_market_impact_formula`
  - [ ] M3-T3 实现成本校准模式与报告行
    - **目标**: 在 `CostModel.fit_calibration` 实现 train-only amount/turnover_rate/sigma20 分桶校准与 fallback。
    - **涉及**: `src/envs/cost_model.py`、`tests/test_cost_model.py`
    - **完成标准**: 样本不足按配置 fallback 并记录 `fallback_reason`；`cost_model.mode="empirical_default"` 时 `cost_calibration_report.csv` 写入 `status="not_applicable"`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_cost_model.py::test_calibrated_cost_report_schema`
  - [ ] M3-T4 实现 `ConstraintManager.project` 基础硬约束
    - **目标**: 在 `src/envs/constraint_manager.py` 实现 availability、long-only、simplex、max_weight、min_weight 投影。
    - **涉及**: `src/envs/constraint_manager.py`、`tests/test_constraint_manager.py`
    - **完成标准**: 不可用资产权重恒为 0；无可用资产且 cash disabled 抛出 `ERR_CONSTRAINT_NO_AVAILABLE_ASSET`；不可行 max/min 自动放宽并记录 violation。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_constraint_manager.py::test_simplex_and_availability_projection`
  - [ ] M3-T5 实现 turnover、HHI、asset-class 与 soft/Lagrangian 约束
    - **目标**: 扩展 `ConstraintManager.project` 支持 `turnover_limit`、HHI、asset-class exposure、soft penalty、PPO-Lagrangian violation 标量。
    - **涉及**: `src/envs/constraint_manager.py`、`tests/test_constraint_manager.py`
    - **完成标准**: 配置 turnover_limit 且 `reference_weights is None` 抛出 `ERR_CONSTRAINT_REFERENCE_WEIGHTS_REQUIRED`；asset class 元数据缺失按配置 skip 或 fail。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_constraint_manager.py::test_turnover_limit_requires_reference_weights`
  - [ ] M3-T6 实现再平衡调度器 pre/post/check
    - **目标**: 在 `src/envs/rebalance_scheduler.py` 实现 `pre_check`、`post_check`、`should_rebalance`，覆盖所有调度模式。
    - **涉及**: `src/envs/rebalance_scheduler.py`、`tests/test_rebalance_scheduler.py`
    - **完成标准**: threshold 模式在 `candidate_weights is None` 时返回 false；最终交易动作只允许 `scheduler_allowed and gate_action == 1`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_rebalance_scheduler.py`
  - [ ] M3-T7 实现执行收益构造工具
    - **目标**: 根据 `execution_model.execution_price` 构造 `ExecutionMarketState.return_from_decision_to_execution` 与 `holding_simple_return`。
    - **涉及**: `src/envs/portfolio_execution_core.py`、`src/data/loader.py`、`tests/test_execution_core.py`
    - **完成标准**: next_open 使用 `open[t+1]/close[t]-1` 与 `close[t+1]/open[t+1]-1`；next_close 使用延迟动作日期；same close debug 写入 idealized 标志。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_execution_core.py::test_next_open_execution_returns tests/test_execution_core.py::test_next_close_delayed_returns`
  - [ ] M3-T8 实现 `sanitize_execution_returns` 与 `drift_weights`
    - **目标**: 在 `PortfolioExecutionCore` 内实现缺失收益填充、冻结资产记录和权重漂移。
    - **涉及**: `src/envs/portfolio_execution_core.py`、`tests/test_execution_core.py`
    - **完成标准**: 持仓资产可交易但收益缺失抛出 `ERR_EXECUTION_RETURN_MISSING`；不可交易冻结资产允许收益填 0 并写入 `ExecutionResult.info["return_imputation"]`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_execution_core.py::test_missing_execution_return_policy`
  - [ ] M3-T9 实现两阶段 NAV 主流程
    - **目标**: 在 `PortfolioExecutionCore.execute_step` 实现成交前收益、漂移权重、执行成本、成交后收益、NAV、收益字段更新。
    - **涉及**: `src/envs/portfolio_execution_core.py`、`tests/test_execution_core.py`
    - **完成标准**: 输出 `pre_execution_return`、`post_execution_return`、`gross_return`、`transaction_cost_on_initial_nav`、`net_return`、`portfolio_log_return`；`nav_next <= 0` 抛出 `ERR_EXECUTION_INVALID_NAV`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_execution_core.py::test_two_stage_nav_formula`
  - [ ] M3-T10 实现执行日可交易性、部分调仓与 T+1 约束
    - **目标**: 在执行阶段二次检查 `tradeable_mask_at_execution`，支持 `rebalance_intensity` 插值与 `t_plus_one` 冻结。
    - **涉及**: `src/envs/portfolio_execution_core.py`、`tests/test_execution_core.py`
    - **完成标准**: 不可买入资产不得新建仓；不可卖资产保持冻结权重；`partial_rebalance_post_check_policy` 三种策略均可触发并记录 violation。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_execution_core.py::test_partial_rebalance_and_t_plus_one_freeze`
  - [ ] M3-T11 实现奖励计算器
    - **目标**: 在 `src/envs/reward_calculator.py::RewardCalculator.calculate` 实现 A0-A12 奖励变体、默认风险惩罚与 differential Sharpe 状态。
    - **涉及**: `src/envs/reward_calculator.py`、`tests/test_reward_calculator.py`
    - **完成标准**: 默认 reward 使用 net log return；A2 不重复扣成本；A7 episode reset A/B 状态；CVaR confidence 映射统一。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_reward_calculator.py`
  - [ ] M3-T12 前置定义 `BaseStrategy` 策略接口合同
    - **目标**: 在 `src/baselines/base_strategy.py` 定义 `BaseStrategy.fit`、`reset`、`compute_target_weights` 合同，并复用 `src/envs/state.py::PortfolioAction`。
    - **涉及**: `src/baselines/base_strategy.py`、`src/envs/state.py`、`tests/test_baselines.py`
    - **完成标准**: `compute_target_weights` 只能接收 `DecisionMarketState` 并返回 `PortfolioAction(target_weights, rebalance_action, rebalance_intensity, action_info)`；接口文件不实现任何具体策略逻辑。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_base_strategy_contract`
  - [ ] M3-T13 实现 `BacktestEngine.run` 非延迟执行链路
    - **目标**: 在 `src/envs/backtest_engine.py` 实现日期循环、状态构造、scheduler、strategy、execution core、reward、日度记录。
    - **涉及**: `src/envs/backtest_engine.py`、`tests/test_backtest_engine.py`
    - **完成标准**: `initial_build_cost` 两种模式可测；策略优化失败写入 `action_info["fallback_reason"]`；validation/test 不更新 fit 统计量。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_backtest_engine.py::test_backtest_next_open_outputs_daily_records`
  - [ ] M3-T14 实现 `PendingAction` 延迟执行队列
    - **目标**: 在 `BacktestEngine` 与环境中支持 `delayed_action_execution=true` 的排队、出队、reward/action 对齐。
    - **涉及**: `src/envs/backtest_engine.py`、`src/envs/portfolio_rebalance_env.py`、`tests/test_backtest_engine.py`
    - **完成标准**: `PendingAction` 记录 decision/execution/valuation 日期与 q/value/action_info；split 末尾执行日期越界按 `ERR_EXECUTION_DATE_OUT_OF_RANGE` 截断，不补虚拟收益。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_backtest_engine.py::test_pending_action_queue_alignment`
  - [ ] M3-T15 实现 Gymnasium 1.x 环境
    - **目标**: 在 `src/envs/portfolio_rebalance_env.py` 实现 `reset`、`step`、observation_space、action_space。
    - **涉及**: `src/envs/portfolio_rebalance_env.py`、`tests/test_env.py`
    - **完成标准**: `reset()` 返回 `(obs, info)`；`step()` 返回 `(obs, reward, terminated, truncated, info)`；split 正常结束 `terminated=false,truncated=true`；非法 action 抛出 `ERR_ACTION_NON_FINITE` 或 `ERR_ACTION_SHAPE_MISMATCH`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_env.py::test_gymnasium_reset_step_signature`
  - [ ] M3-T16 实现决策态/执行态可见性隔离测试钩子
    - **目标**: 在环境、回测、leakage checks 中确保策略、actor、critic、DQN gate 只能访问 `DecisionMarketState`。
    - **涉及**: `src/envs/portfolio_rebalance_env.py`、`src/envs/backtest_engine.py`、`src/data/leakage_checks.py`、`tests/test_env.py`
    - **完成标准**: `holding_simple_return`、`return_from_decision_to_execution`、execution_date 后 amount/volume/availability 不进入 observation、market_image、gate_input、feature_window。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_env.py::test_execution_only_field_leakage`

## 里程碑 M4: 传统策略可统一回测
- **状态**: 部分完成
- **阶段性成果**: 所有传统资产配置策略均通过 `BaseStrategy` 接入 `BacktestEngine`、`RebalanceScheduler`、`CostModel`、`ConstraintManager`。
- **完成判定**: `.venv/bin/python -m pytest tests/test_baselines.py tests/test_baseline_output_schema.py::test_traditional_baseline_output_schema` 全部通过；每个传统基准可在小样本数据上输出标准 `PortfolioAction` 或标准回测记录。
- **任务清单**:
  - [ ] M4-T1 接入传统策略 `BaseStrategy` 继承约束
    - **目标**: 在传统策略实现与 `tests/test_baselines.py` 中验证所有传统策略继承 `BaseStrategy` 并复用 `src/envs/state.py::PortfolioAction`。
    - **涉及**: `src/baselines/base_strategy.py`、`tests/test_baselines.py`
    - **完成标准**: fixed_ratio、equal_weight、buy_and_hold、markowitz、risk_parity、inverse_volatility、minimum_drawdown、risk_evaluation、hrp、momentum 均返回 `PortfolioAction(target_weights, rebalance_action, rebalance_intensity, action_info)`；策略只能接收 `DecisionMarketState`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_traditional_strategies_inherit_base_strategy`
  - [ ] M4-T2 实现固定比例策略
    - **目标**: 在 `src/baselines/fixed_ratio.py` 实现资产权重、资产类别权重、类别内等权/逆波动分配。
    - **涉及**: `src/baselines/fixed_ratio.py`、`tests/test_baselines.py`
    - **完成标准**: 类别无可用资产时按其他可用类别重分配；输出 `logs/fixed_ratio_weights.json` 所需结构；目标权重经过 `ConstraintManager`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_fixed_ratio_category_reallocation`
  - [ ] M4-T3 实现等权重与买入持有策略
    - **目标**: 在 `equal_weight.py` 与 `buy_and_hold.py` 实现可用资产等权、首日建仓后持有、forced rebalance。
    - **涉及**: `src/baselines/equal_weight.py`、`src/baselines/buy_and_hold.py`、`tests/test_baselines.py`
    - **完成标准**: 不可用资产权重为 0；非再平衡日返回持有动作；buy_and_hold 支持 `initial_weight_mode` 与 `reset_frequency`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_equal_weight_and_buy_hold`
  - [ ] M4-T4 实现三类 Markowitz 策略
    - **目标**: 在 `src/baselines/markowitz.py` 实现 `traditional_markowitz_mean_variance`、`markowitz_min_variance`、`markowitz_max_sharpe`。
    - **涉及**: `src/baselines/markowitz.py`、`src/utils/optimization.py`、`tests/test_baselines.py`
    - **完成标准**: 只使用 lookback 历史窗口；支持 Ledoit-Wolf 或 shrinkage；优化失败 fallback 并记录 `fallback_reason`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_markowitz_strategies_fallback`
  - [ ] M4-T5 实现风险平价与逆波动率策略
    - **目标**: 在 `risk_parity.py` 与 `inverse_volatility.py` 实现风险贡献优化和逆波动率权重。
    - **涉及**: `src/baselines/risk_parity.py`、`src/baselines/inverse_volatility.py`、`tests/test_baselines.py`
    - **完成标准**: 协方差只使用历史窗口；波动率下限生效；输出权重经过 `ConstraintManager`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_risk_parity_inverse_volatility`
  - [ ] M4-T6 实现最小回撤策略
    - **目标**: 在 `src/baselines/minimum_drawdown.py` 实现 random search + SLSQP 或等价低回撤组合搜索。
    - **涉及**: `src/baselines/minimum_drawdown.py`、`tests/test_baselines.py`
    - **完成标准**: 目标函数包含 MDD、volatility、mean return；有效资产不足或优化失败 fallback。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_minimum_drawdown_strategy`
  - [ ] M4-T7 实现风险评价策略
    - **目标**: 在 `src/baselines/risk_evaluation.py` 实现 return、volatility、MDD、downside、CVaR、liquidity 综合评分。
    - **涉及**: `src/baselines/risk_evaluation.py`、`tests/test_baselines.py`
    - **完成标准**: turnover_rate 全空时使用 `amount / ADV20` 替代；缺流动性时记录 `fallback_reason="liquidity_unavailable"` 并使用中性分数。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_risk_evaluation_liquidity_fallback`
  - [ ] M4-T8 实现 HRP 策略
    - **目标**: 在 `src/baselines/hrp.py` 实现相关性距离、层次聚类、quasi-diagonalization、递归二分。
    - **涉及**: `src/baselines/hrp.py`、`tests/test_baselines.py`
    - **完成标准**: 使用历史收益矩阵；不可用资产权重为 0；输出权重经过 `ConstraintManager`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_hrp_strategy`
  - [ ] M4-T9 实现动量策略
    - **目标**: 在 `src/baselines/momentum.py` 实现 momentum、risk-adjusted momentum、top_k、threshold 与权重模式。
    - **涉及**: `src/baselines/momentum.py`、`tests/test_baselines.py`
    - **完成标准**: 只使用 decision_date 之前历史；支持 equal、inverse_volatility、softmax(score) 权重；无候选资产 fallback。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_momentum_strategy`
  - [ ] M4-T10 验证传统基准输出 schema
    - **目标**: 编写传统基准回测小样本测试，确保每个传统策略经 `BacktestEngine` 输出冻结 daily schema。
    - **涉及**: `tests/test_baseline_output_schema.py`
    - **完成标准**: 每个传统策略均输出 `daily_returns`、`daily_weights`、`daily_turnover`、`daily_rebalance`、`daily_costs` 必需列。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baseline_output_schema.py::test_traditional_baseline_output_schema`

## 里程碑 M5: 模型组件、强化学习模块与深度基准适配可单元验证
- **状态**: 部分完成
- **阶段性成果**: CNN/Transformer/TCN/MLP encoder、PPO actor/critic、DQN gate、辅助头、偏好条件化、不确定性、分布式 CVaR、部分调仓模块均可独立前向、mask、loss 与 shape 校验，深度/RL 基准可在模型合同就绪后通过统一适配层输出标准 action。
- **完成判定**: `.venv/bin/python -m pytest tests/test_models.py tests/test_ppo_actor.py tests/test_dqn_gate.py tests/test_preference.py tests/test_uncertainty.py tests/test_distributional.py tests/test_partial_rebalance.py tests/test_baselines.py::test_ppo_baseline_action_contract tests/test_baselines.py::test_gated_and_dqn_only_baseline_contract tests/test_baselines.py::test_eiie_baseline_masking tests/test_baseline_output_schema.py::test_deep_baseline_output_schema` 全部通过。
- **任务清单**:
  - [ ] M5-T1 实现 encoder family
    - **目标**: 在 `src/models/encoders.py` 实现 CNN、CNN+attention、Temporal Transformer、TCN、MLP fallback。
    - **涉及**: `src/models/encoders.py`、`tests/test_models.py`
    - **完成标准**: 输入 `[batch,n_features,window_size,n_assets]` 输出 `[batch,latent_dim]`；默认 latent_dim=256；支持 LayerNorm、GELU、Dropout。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_models.py::test_encoder_family_shapes`
  - [ ] M5-T2 实现 PPO Dirichlet actor
    - **目标**: 在 `src/models/ppo_actor.py::PPOActor` 实现 available assets 子空间 Dirichlet、采样、deterministic mean、log_prob。
    - **涉及**: `src/models/ppo_actor.py`、`tests/test_ppo_actor.py`
    - **完成标准**: 不从全资产 Dirichlet 采样后裁剪；不可用资产权重为 0；alpha 非 finite 抛错；单可用资产 log_prob 为 0 或跳过 actor loss。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_ppo_actor.py::test_dirichlet_mask_semantics`
  - [ ] M5-T3 实现 Beta 强度 actor
    - **目标**: 在 `src/models/ppo_actor.py` 或 `src/models/partial_rebalance_gate.py` 实现 Beta(a,b) 强度输出与 log_prob。
    - **涉及**: `src/models/ppo_actor.py`、`src/models/partial_rebalance_gate.py`、`tests/test_partial_rebalance.py`
    - **完成标准**: `a_raw,b_raw -> softplus + beta_min_concentration`；rho 位于 `[0,1]`；训练 log_prob 使用未裁剪样本。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_partial_rebalance.py::test_beta_rebalance_intensity_distribution`
  - [ ] M5-T4 实现 PPO critic 与 distributional critic 基础
    - **目标**: 在 `ppo_critic.py` 输出 `V(state_t)`；在 `distributional_critic.py` 输出 quantile values。
    - **涉及**: `src/models/ppo_critic.py`、`src/models/distributional_critic.py`、`tests/test_models.py`、`tests/test_distributional.py`
    - **完成标准**: PPO critic 支持 clipped value loss 输入；distributional critic 输出 `[batch,n_quantiles]` 并提供 CVaR/tail loss 计算。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_distributional.py::test_quantile_cvar_shapes`
  - [ ] M5-T5 实现 DQN gate 网络
    - **目标**: 在 `src/models/dqn_gate.py` 实现 binary gate、dueling、Double DQN target helper、N-step 接口、Q-gap threshold、cooldown/hysteresis 开关。
    - **涉及**: `src/models/dqn_gate.py`、`tests/test_dqn_gate.py`
    - **完成标准**: 默认 output_dim=2；partial discrete 模式按 rho 档位输出；gate input 包含 candidate weights、estimated_turnover、estimated_cost 且不含 execution-only 字段。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_dqn_gate.py::test_gate_input_contract`
  - [ ] M5-T6 实现辅助预测头与损失汇总
    - **目标**: 在 `src/models/auxiliary_heads.py` 实现 future return、volatility、trend、rank、downside、max drawdown、CVaR、covariance、masked reconstruction heads。
    - **涉及**: `src/models/auxiliary_heads.py`、`tests/test_models.py`
    - **完成标准**: `L_representation_regularization = mean(z_t ** 2)` 且不 detach；支持 `[batch,latent_dim]` 与 `[batch,n_assets,latent_dim]` mask 口径。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_models.py::test_auxiliary_loss_contract`
  - [ ] M5-T7 实现组合主模型 `Full DQN-Gated Multi-Task CNN-PPO`
    - **目标**: 在 `src/models/dqn_gated_multitask_cnn_ppo.py` 组合 encoder、PPO actor、PPO critic、DQN gate、auxiliary heads。
    - **涉及**: `src/models/dqn_gated_multitask_cnn_ppo.py`、`tests/test_models.py`
    - **完成标准**: 前向返回 candidate_weights、log_prob、value、gate_q、gate_action、estimated_turnover 输入槽、aux outputs。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_models.py::test_full_gated_model_forward_contract`
  - [ ] M5-T8 实现偏好条件化模块
    - **目标**: 在 `src/models/preference_conditioner.py` 与 `preference_conditioned_gated_ppo.py` 支持 omega 融合与多目标 reward vector。
    - **涉及**: `src/models/preference_conditioner.py`、`src/models/preference_conditioned_gated_ppo.py`、`tests/test_preference.py`
    - **完成标准**: omega 非负且 sum=1；actor、critic、gate 均接收 conditioned latent；固定评估偏好集合可生成。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_preference.py`
  - [ ] M5-T9 实现不确定性模块
    - **目标**: 在 `src/models/uncertainty_heads.py` 与 `uncertainty_aware_gated_ppo.py` 实现 multi-head/dropout 不确定性估计。
    - **涉及**: `src/models/uncertainty_heads.py`、`src/models/uncertainty_aware_gated_ppo.py`、`tests/test_uncertainty.py`
    - **完成标准**: 输出 mean candidate weights、candidate weight variance、q_uncertainty、weight_uncertainty；gate input 扩展不含 execution-only 字段。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_uncertainty.py`
  - [ ] M5-T10 实现 Distributional-CVaR 模块
    - **目标**: 在 `distributional_cvar_gated_ppo.py` 使用 distributional critic 计算 candidate/hold 的 expected value、CVaR、tail loss、delta_U。
    - **涉及**: `src/models/distributional_cvar_gated_ppo.py`、`src/models/distributional_critic.py`、`tests/test_distributional.py`
    - **完成标准**: `cvar_alpha=0.05` 与 reward/metrics 一致；quantile regression Huber loss 可计算；gate risk features 完整。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_distributional.py::test_distributional_cvar_gate_features`
  - [ ] M5-T11 实现部分调仓 gate 模块
    - **目标**: 在 `src/models/partial_rebalance_gate.py` 与 `partial_rebalance_gated_ppo.py` 实现 discrete DQN、continuous Beta、hybrid DQN+Beta 三种模式。
    - **涉及**: `src/models/partial_rebalance_gate.py`、`src/models/partial_rebalance_gated_ppo.py`、`tests/test_partial_rebalance.py`
    - **完成标准**: discrete rho values 默认 `[0,0.25,0.5,0.75,1]`；binary/hybrid gate output_dim=2；PPO joint log_prob 支持 Dirichlet+Beta。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_partial_rebalance.py::test_partial_gate_modes`
  - [ ] M5-T12 实现估算成本特征计算
    - **目标**: 在模型或 agent helper 中用 `DecisionMarketState` 可见字段估算 `estimated_turnover_t` 与 `estimated_cost_t`。
    - **涉及**: `src/models/dqn_gated_multitask_cnn_ppo.py`、`src/agents/hybrid_agent.py`、`tests/test_dqn_gate.py`
    - **完成标准**: estimated cost 使用 decision_date 可见 ADV20、sigma20、portfolio_value；calibrated 模式只用 train-fitted calibration table；回测 NAV 不使用 estimated cost。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_dqn_gate.py::test_estimated_cost_uses_decision_state_only`
  - [ ] M5-T13 实现 PPO-only 与 CNN-PPO 基准策略适配
    - **目标**: 在 `ppo_baseline.py`、`cnn_ppo_baseline.py` 复用 `src/models/encoders.py` 与 `src/models/ppo_actor.py::PPOActor` 输出，并包装为标准 `PortfolioAction`。
    - **涉及**: `src/baselines/ppo_baseline.py`、`src/baselines/cnn_ppo_baseline.py`、`src/models/encoders.py`、`src/models/ppo_actor.py`、`tests/test_baselines.py`
    - **完成标准**: PPO candidate weights 仍受 scheduler 与约束控制；不可用资产不参与 Dirichlet 分布；输出 action 字段满足 `BaseStrategy`/agent 适配合同。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_ppo_baseline_action_contract`
  - [ ] M5-T14 实现 Bernoulli-Gated PPO 与 DQN-only 基准适配
    - **目标**: 在 `bernoulli_gated_ppo.py`、`dqn_only.py` 复用 `src/models/ppo_actor.py::PPOActor` 与 `src/models/dqn_gate.py::DQNGate`，实现 gate 动作、离散模板选择与标准 action 输出。
    - **涉及**: `src/baselines/bernoulli_gated_ppo.py`、`src/baselines/dqn_only.py`、`src/models/ppo_actor.py`、`src/models/dqn_gate.py`、`tests/test_baselines.py`
    - **完成标准**: Bernoulli gate 保存 log_prob；DQN-only 不直接输出连续权重，只通过模板生成目标权重；gate input 不含 execution-only 字段。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_gated_and_dqn_only_baseline_contract`
  - [ ] M5-T15 实现 EIIE 基准适配
    - **目标**: 在 `src/baselines/eiie.py` 实现共享 evaluator score、availability mask softmax 与权重输出，并复用 M5 encoder 输入合同。
    - **涉及**: `src/baselines/eiie.py`、`src/models/encoders.py`、`tests/test_baselines.py`
    - **完成标准**: unavailable asset score 置为不可选；支持 previous_weights；输出日度权重、换手、成本字段。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py::test_eiie_baseline_masking`
  - [ ] M5-T16 验证深度/RL 基准输出 schema
    - **目标**: 编写深度/RL 基准回测小样本测试，确保每个深度基准经统一适配层输出冻结 daily schema。
    - **涉及**: `tests/test_baseline_output_schema.py`
    - **完成标准**: PPO-only、CNN-PPO、Bernoulli-Gated PPO、DQN-only、EIIE 均输出 `daily_returns`、`daily_weights`、`daily_turnover`、`daily_rebalance`、`daily_costs` 必需列。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baseline_output_schema.py::test_deep_baseline_output_schema`

## 里程碑 M6: 训练缓存、Agent 与 checkpoint 可恢复
- **状态**: 部分完成
- **阶段性成果**: PPO rollout、DQN replay/PER、PPO/DQN/Hybrid/specialized agents、设备迁移、checkpoint save/load、deterministic evaluation 构成可恢复训练闭环。
- **完成判定**: `.venv/bin/python -m pytest tests/test_buffers.py tests/test_agents.py tests/test_checkpoint.py tests/test_deterministic.py` 全部通过；checkpoint 可恢复同一 evaluation 输出。
- **任务清单**:
  - [ ] M6-T1 实现 PPO rollout buffer
    - **目标**: 在 `src/buffers/rollout_buffer.py` 定义 `RolloutItem` 与 `RolloutBuffer`，保存 PPO 训练必需字段。
    - **涉及**: `src/buffers/rollout_buffer.py`、`tests/test_buffers.py`
    - **完成标准**: 每项保存 decision/execution/valuation 日期、execution_price、delayed_action_execution、state、candidate/executed weights、log_prob、value、reward、terminated、truncated、advantage、return、auxiliary labels。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_buffers.py::test_rollout_buffer_required_fields`
  - [ ] M6-T2 实现 DQN replay buffer
    - **目标**: 在 `src/buffers/replay_buffer.py` 定义 `ReplayItem` 与 N-step 截断逻辑。
    - **涉及**: `src/buffers/replay_buffer.py`、`tests/test_buffers.py`
    - **完成标准**: 保存当前与下一状态的 decision/execution/valuation 日期、execution_price、delayed_action_execution、estimated/realized cost、q values；terminal/truncated/split boundary 截断 N-step。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_buffers.py::test_replay_buffer_date_fields_and_n_step`
  - [ ] M6-T3 实现 Prioritized Replay Buffer
    - **目标**: 在 `src/buffers/prioritized_replay_buffer.py` 实现 PER priority、sampling probability、IS weight、beta annealing。
    - **涉及**: `src/buffers/prioritized_replay_buffer.py`、`tests/test_buffers.py`
    - **完成标准**: priority 公式为 `(abs(td_error)+per_priority_eps)**per_alpha`；IS weight 归一化到 batch max。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_buffers.py::test_prioritized_replay_formula`
  - [ ] M6-T4 实现 PPO agent 更新
    - **目标**: 在 `src/agents/ppo_agent.py` 实现 rollout collection、GAE、clipped actor loss、clipped value loss、entropy、gradient clipping。
    - **涉及**: `src/agents/ppo_agent.py`、`tests/test_agents.py`
    - **完成标准**: advantage normalization 默认 true；gate=hold 样本默认不进 actor loss；partial rho 作为 actor_loss_weight。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_agents.py::test_ppo_update_contract`
  - [ ] M6-T5 实现 DQN agent 更新
    - **目标**: 在 `src/agents/dqn_agent.py` 实现 Double DQN target、target network 更新、epsilon schedule、PER priority 更新。
    - **涉及**: `src/agents/dqn_agent.py`、`tests/test_agents.py`
    - **完成标准**: target 使用 online argmax + target value；terminal/truncated 不 bootstrap；支持 hard update 与 soft tau。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_agents.py::test_double_dqn_target`
  - [ ] M6-T6 实现 HybridAgent 训练主循环
    - **目标**: 在 `src/agents/hybrid_agent.py::HybridAgent.train` 串联 env rollout、PPO update、DQN update、auxiliary update、validation evaluation。
    - **涉及**: `src/agents/hybrid_agent.py`、`tests/test_agents.py`
    - **完成标准**: 默认 encoder 由 PPO + auxiliary 更新，DQN latent 默认 detach；训练异常写入失败状态；validation deterministic evaluation 用于 checkpoint 选择。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_agents.py::test_hybrid_agent_single_update_smoke`
  - [ ] M6-T7 实现 specialized agents 封装
    - **目标**: 在 `preference_agent.py`、`uncertainty_agent.py`、`distributional_agent.py`、`partial_rebalance_agent.py` 封装各模块专属训练逻辑。
    - **涉及**: `src/agents/preference_agent.py`、`src/agents/uncertainty_agent.py`、`src/agents/distributional_agent.py`、`src/agents/partial_rebalance_agent.py`、`tests/test_agents.py`
    - **完成标准**: 各 agent 复用 `HybridAgent` 公共执行、成本、约束、输出契约，不新增另一套 NAV 或成本逻辑。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_agents.py::test_specialized_agents_share_execution_contract`
  - [ ] M6-T8 实现 checkpoint 保存与安全加载
    - **目标**: 在 `src/utils/checkpoint.py` 实现 `save_checkpoint`、`load_checkpoint`、safe torch load。
    - **涉及**: `src/utils/checkpoint.py`、`tests/test_checkpoint.py`
    - **完成标准**: payload 包含 encoder、actor、critic、dqn gate、target network、aux heads、optimizers、schedulers、GradScaler、replay buffer、epoch、global_step、best metric、RNG、resolved_config；CPU/MPS GradScaler 为 null。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_checkpoint.py::test_checkpoint_payload_schema`
  - [ ] M6-T9 实现随机种子与确定性评估
    - **目标**: 在 `src/utils/seed.py` 与 agents 中记录 Python、NumPy、Torch CPU/CUDA、环境随机状态。
    - **涉及**: `src/utils/seed.py`、`tests/test_deterministic.py`
    - **完成标准**: 相同 seed 与 checkpoint 恢复后 deterministic evaluation 输出一致；`cudnn_benchmark=false`、`cudnn_deterministic=true` 可配置。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_deterministic.py`
  - [ ] M6-T10 实现训练稳定性守卫
    - **目标**: 在 agent update 前后检测 loss、alpha、grad NaN/Inf 并记录日志。
    - **涉及**: `src/agents/*.py`、`src/utils/logger.py`、`tests/test_agents.py`
    - **完成标准**: 非 finite loss fail fast；梯度裁剪使用 `max_grad_norm`；异常时保留已有日志并将 run registry 状态置 failed。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_agents.py::test_non_finite_training_guard`

## 里程碑 M7: 实验编排、输出归档与统计评估可复现
- **状态**: 部分完成
- **阶段性成果**: 单实验、多实验、HPO、消融、walk-forward、聚合、指标、图表、统计检验、run registry 和固定输出 schema 全部可被统一入口调用。
- **完成判定**: `.venv/bin/python -m pytest tests/test_experiments.py tests/test_outputs.py tests/test_metrics.py tests/test_plotting.py tests/test_stats.py tests/test_registry.py` 全部通过；小样本 main run 生成所有必需 CSV/log schema。
- **任务清单**:
  - [ ] M7-T1 实现实验注册表
    - **目标**: 在 `src/experiments/registry.py::ExperimentRegistry.create_experiment` 按 `experiment.type` 创建 main、baseline、ablation、walk_forward、HPO 等实验对象。
    - **涉及**: `src/experiments/registry.py`、`tests/test_experiments.py`
    - **完成标准**: 未知实验类型 fail fast；所有实验复用同一 `PortfolioExecutionCore`、`CostModel`、`ConstraintManager`、输出 schema。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_experiments.py::test_registry_all_experiment_types`
  - [ ] M7-T2 完成 `run_experiment.py` 执行闭环
    - **目标**: 扩展 `run_experiment.py::main` 完成 create experiment、run、write outputs、manifest。
    - **涉及**: `src/experiments/run_experiment.py`、`tests/test_experiments.py`
    - **完成标准**: `--seed`、`--device`、`--output`、`--run-name` 覆盖生效；HPO enabled 时进入 HPO runner；失败时 registry 记录 failed。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_experiments.py::test_run_experiment_smoke`
  - [ ] M7-T3 实现全量实验矩阵入口
    - **目标**: 在 `src/experiments/run_all.py::run_experiment_matrix` 按 full_reproduction 顺序执行主模型、基准、消融、敏感性、walk-forward、统计检验。
    - **涉及**: `src/experiments/run_all.py`、`tests/test_experiments.py`
    - **完成标准**: 不新增业务逻辑；只组合已有 registry 实验；每个子 run 有 parent_run_id lineage。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_experiments.py::test_run_all_matrix_builds_lineage`
  - [ ] M7-T4 实现 HPO trial 隔离
    - **目标**: 在 `run_experiment.py::run_hpo` 使用 Optuna TPE、median pruner、固定 seed 与 validation objective。
    - **涉及**: `src/experiments/run_experiment.py`、`tests/test_experiments.py`
    - **完成标准**: trial 只用 train/validation；test 只在 best trial 锁定后执行一次；每个 trial 写入 `hpo_trials.csv` 必需字段。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_experiments.py::test_hpo_trial_isolation`
  - [ ] M7-T5 实现消融实验单开关校验
    - **目标**: 在实验编排中校验每个 ablation run 只修改一个开关或一个参数族。
    - **涉及**: `src/experiments/registry.py`、`src/experiments/run_all.py`、`tests/test_experiments.py`
    - **完成标准**: 多非同族 key 修改抛出 `ERR_EXPERIMENT_ABLATION_NOT_SINGLE_SWITCH`；消融不移除真实交易成本扣除。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_experiments.py::test_ablation_single_switch_guard`
  - [ ] M7-T6 实现 walk-forward 聚合
    - **目标**: 在 `src/experiments/aggregate_results.py::aggregate_walk_forward` 拼接 fold 样本外 daily net returns 并计算 all_oos 指标。
    - **涉及**: `src/experiments/aggregate_results.py`、`tests/test_experiments.py`
    - **完成标准**: fold daily 输出保留 `fold_id`；日期重叠保留第一条并记录 `duplicate_oos_date_count`；all_oos 行写入 `walk_forward_results.csv`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_experiments.py::test_walk_forward_all_oos_aggregation`
  - [ ] M7-T7 实现绩效指标
    - **目标**: 在 `src/utils/metrics.py::calculate_performance_metrics` 实现累计收益、年化收益、波动、Sharpe、Sortino、Calmar、Omega、MDD、VaR、CVaR、turnover、cost、hit ratio 等指标。
    - **涉及**: `src/utils/metrics.py`、`tests/test_metrics.py`
    - **完成标准**: 使用 `daily_returns.csv.net_return`；drawdown 同时支持 signed/abs 口径；年化因子默认 252。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_metrics.py`
  - [ ] M7-T8 实现固定输出 CSV schema
    - **目标**: 在 `src/utils/logger.py::write_run_outputs` 输出 daily_returns、daily_weights、daily_turnover、daily_rebalance、daily_costs、metrics 文件。
    - **涉及**: `src/utils/logger.py`、`tests/test_outputs.py`
    - **完成标准**: 所有冻结字段存在；`date == next_valuation_date`；无 q values 的模型保留空列；专项未启用时写入空 schema 或 `status="not_applicable"`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_outputs.py::test_daily_output_schema`
  - [ ] M7-T9 实现 run manifest 与 asset/log 输出
    - **目标**: 在 `write_run_outputs` 写入 `run_manifest.json`、`asset_list.txt`、`data_split.json`、`config_snapshot.yaml`、`feature_*`、`pca_report.json` 等日志。
    - **涉及**: `src/utils/logger.py`、`tests/test_outputs.py`
    - **完成标准**: manifest 包含 execution_model、data_governance、initial capital、currency、execution_price、delayed_action_execution、idealized flags、strict_no_lookahead、t_plus_one、amount/proxy、turnover_rate 标志。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_outputs.py::test_run_manifest_frozen_fields`
  - [ ] M7-T10 实现本地 run registry
    - **目标**: 在 `src/utils/logger.py` 或 `src/experiments/registry.py` 创建 `run_registry.sqlite` 的 runs、artifacts、metrics、lineage 表。
    - **涉及**: `src/utils/logger.py`、`tests/test_registry.py`
    - **完成标准**: run 状态从 `running` 更新为 `success` 或 `failed`；artifact 路径、hash、created_at 可查询；无外部数据库依赖。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_registry.py`
  - [ ] M7-T11 实现图表输出
    - **目标**: 在 `src/utils/plotting.py` 生成详细设计冻结的 figures 文件名。
    - **涉及**: `src/utils/plotting.py`、`tests/test_plotting.py`
    - **完成标准**: 必须生成固定文件 `equity_curve.png`、`drawdown_curve_abs.png`、`drawdown_curve_signed.png`、`rolling_return_curve.png`、`rolling_volatility_curve.png`、`rolling_Sharpe_curve.png`、`rolling_Sortino_curve.png`、`return_distribution_histogram.png`、`return_qq_plot.png`、`VaR_CVaR_tail_curve.png`、`portfolio_weight_heatmap.png`、`portfolio_weight_stack_area.png`、`asset_class_exposure_stack_area.png`、`rebalance_timeline.png`、`turnover_curve.png`、`transaction_cost_curve.png`、`cost_breakdown_stack_area.png`、`risk_contribution_bar.png`、`HHI_concentration_curve.png`、`correlation_heatmap.png`、`covariance_heatmap.png`、`train_reward_curve.png`、`validation_reward_curve.png`、`episodic_return_curve.png`、`PPO_actor_loss_curve.png`、`PPO_critic_loss_curve.png`、`PPO_entropy_curve.png`、`PPO_approx_kl_curve.png`、`PPO_clip_fraction_curve.png`、`PPO_advantage_distribution.png`、`DQN_Q_value_curve.png`、`DQN_Q_gap_curve.png`、`DQN_TD_error_curve.png`、`DQN_epsilon_curve.png`、`DQN_gate_action_ratio_curve.png`、`auxiliary_prediction_loss_curve.png`、`gradient_norm_curve.png`、`learning_rate_curve.png`、`policy_concentration_curve.png`、`constraint_violation_curve.png`、`input_matrix_validation_score_bar.png`、`PCA_explained_variance_curve.png`、`PCA_component_sensitivity_curve.png`、`preference_frontier.png`、`uncertainty_action_heatmap.png`、`cvar_tail_risk_curve.png`、`rebalance_intensity_histogram.png`；条件文件 `DQN_replay_priority_distribution.png`、`attention_heatmap.png` 按启用状态输出或标记 not_applicable。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_plotting.py`
  - [ ] M7-T12 实现统计检验
    - **目标**: 在 `src/utils/stats.py::run_statistical_tests` 实现 paired out-of-sample returns 的 bootstrap、HAC、PSR、DSR、White Reality Check、Hansen SPA、DM、Holm-Bonferroni。
    - **涉及**: `src/utils/stats.py`、`tests/test_stats.py`
    - **完成标准**: 缺失日期取交集且不前向填充；样本不足写 `status="skipped"` 与 `skip_reason="insufficient_samples"`；DM 仅用于 auxiliary forecast error。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_stats.py`
  - [ ] M7-T13 实现原子文件写入与结果目录安全
    - **目标**: 统一所有结果文件写入先临时文件后原子替换，且路径必须在 `results/<run_name>/` 下。
    - **涉及**: `src/utils/logger.py`、`tests/test_outputs.py`
    - **完成标准**: 正式实验入口不得写 `data/processed/`、`data/metrics_factory/`、`data/reports/`；路径越界抛出 `ERR_SECURITY_PATH_DENIED`。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_outputs.py::test_output_paths_are_under_run_dir`

## 里程碑 M8: 端到端验收测试与交付约束闭环
- **状态**: 进行中
- **阶段性成果**: 自动化测试覆盖原始需求第 25 节与详细设计第 6.4 节最低验证要求，开发 Agent 可通过任务单逐项完成并最终执行全量 pytest。
- **完成判定**: `.venv/bin/python -m pytest` 全部通过；测试不扫描 `.venv` 噪声；未修改 `scripts/`、`vendor/`、`requirements_data_pipeline.txt`、`data/**/*.parquet`、`data/reports/*.json`。
- **任务清单**:
  - [ ] M8-T1 建立测试 fixture 与小样本数据工厂
    - **目标**: 在 `tests/conftest.py` 构造小样本 `MarketDatasetBundle`、wide tables、metrics features、split、portfolio state、config fixture。
    - **涉及**: `tests/conftest.py`
    - **完成标准**: fixture 不写真实 `data/` 目录；可构造 turnover_rate 全空、缺文件、无可用资产、execution return 缺失等场景。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py --fixtures`
  - [ ] M8-T2 补齐数据与特征测试套件
    - **目标**: 覆盖 data loader schema、asset order、date split、leakage、MetricsFactory full audit、provenance、audit sample、PCA train-only、availability mask、lazy dataset、auxiliary target。
    - **涉及**: `tests/test_data_loader.py`、`tests/test_feature_matrix.py`、`tests/test_feature_reduction.py`、`tests/test_splits.py`、`tests/test_leakage_checks.py`、`tests/test_auxiliary_targets.py`
    - **完成标准**: 每个冻结错误码至少有一个 fail-fast 测试；不硬编码 `panel_rows`、`all_metrics_rows`、旧日期范围。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_data_loader.py tests/test_feature_matrix.py tests/test_feature_reduction.py tests/test_splits.py tests/test_leakage_checks.py tests/test_auxiliary_targets.py`
  - [ ] M8-T3 补齐执行、环境与回测测试套件
    - **目标**: 覆盖 simplex、cost、reward、scheduler、env signature、two-stage NAV、next_open、next_close、PendingAction、T+1、estimated vs realized cost、portfolio_value market impact。
    - **涉及**: `tests/test_constraint_manager.py`、`tests/test_cost_model.py`、`tests/test_reward_calculator.py`、`tests/test_rebalance_scheduler.py`、`tests/test_execution_core.py`、`tests/test_env.py`、`tests/test_backtest_engine.py`
    - **完成标准**: BacktestEngine 与 PortfolioRebalanceEnv 均证明调用同一个 `PortfolioExecutionCore`；execution-only field leakage 测试失败路径明确。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_constraint_manager.py tests/test_cost_model.py tests/test_reward_calculator.py tests/test_rebalance_scheduler.py tests/test_execution_core.py tests/test_env.py tests/test_backtest_engine.py`
  - [ ] M8-T4 补齐策略、模型与训练测试套件
    - **目标**: 覆盖 traditional strategies、DQN gate input、PPO Dirichlet actor、preference、uncertainty、distributional critic、partial rebalance、buffers、agents、checkpoint、deterministic。
    - **涉及**: `tests/test_baselines.py`、`tests/test_models.py`、`tests/test_ppo_actor.py`、`tests/test_dqn_gate.py`、`tests/test_preference.py`、`tests/test_uncertainty.py`、`tests/test_distributional.py`、`tests/test_partial_rebalance.py`、`tests/test_buffers.py`、`tests/test_agents.py`、`tests/test_checkpoint.py`、`tests/test_deterministic.py`
    - **完成标准**: 所有网络测试使用小 batch；不要求长时间真实训练；shape、mask、loss、字段、checkpoint schema 均有断言。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baselines.py tests/test_models.py tests/test_ppo_actor.py tests/test_dqn_gate.py tests/test_preference.py tests/test_uncertainty.py tests/test_distributional.py tests/test_partial_rebalance.py tests/test_buffers.py tests/test_agents.py tests/test_checkpoint.py tests/test_deterministic.py`
  - [ ] M8-T5 补齐实验输出与统计测试套件
    - **目标**: 覆盖 baseline output schema、experiment output schema、run_manifest frozen fields、walk-forward all_oos、HPO trial isolation、statistics Holm-Bonferroni。
    - **涉及**: `tests/test_baseline_output_schema.py`、`tests/test_experiments.py`、`tests/test_outputs.py`、`tests/test_metrics.py`、`tests/test_plotting.py`、`tests/test_stats.py`、`tests/test_registry.py`
    - **完成标准**: 小样本实验产物包含所有必需 CSV/log schema；专项未启用时 not_applicable 行或空 schema 仍存在。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_baseline_output_schema.py tests/test_experiments.py tests/test_outputs.py tests/test_metrics.py tests/test_plotting.py tests/test_stats.py tests/test_registry.py`
  - [ ] M8-T6 建立正式平台改动范围守卫测试
    - **目标**: 在 `tests/test_scope_guard.py` 中用静态检查确认正式平台不改不该改的文件。
    - **涉及**: `tests/test_scope_guard.py`
    - **完成标准**: 若 `scripts/rebuild_etf_lof_data.py`、`scripts/run_pyfinance_metrics_factory.py`、`vendor/pyfinance/**`、`requirements_data_pipeline.txt`、`data/**/*.parquet`、`data/reports/*.json` 被正式平台任务修改，测试或审查必须失败。
    - **验证方式**: `.venv/bin/python -m pytest tests/test_scope_guard.py`
  - [ ] M8-T7 建立测试发现边界
    - **目标**: 确认 pytest 只发现 `tests/`，避免 `.venv`、`vendor/pyfinance/legacy_rl_scripts` 噪声污染测试路由。
    - **涉及**: `pyproject.toml`、`tests/test_scope_guard.py`
    - **完成标准**: `pytest --collect-only` 不收集 `.venv` 与 vendor legacy 脚本；`testpaths=["tests"]` 生效。
    - **验证方式**: `.venv/bin/python -m pytest --collect-only`
  - [ ] M8-T8 执行全量验收
    - **目标**: 在全部功能完成后运行正式验收命令。
    - **涉及**: 全项目正式平台文件
    - **完成标准**: `.venv/bin/python -m pytest` 全部通过；若 `.venv` 不存在，使用 Python 3.12 创建；禁止使用 `/opt/usr/bin/python3`。
    - **验证方式**: `.venv/bin/python -m pytest`
