protocol_id: otar_v2_s0_20260605
grid_name: otar_actor_policy_hpo_grid
selection_scope: validation_only
no_test_peeking: true
candidate_values:
  concentration_alpha_min: [0.05, 0.10, 0.50]
  concentration_alpha_max: [20.0, 50.0]
  entropy_coef: [0.0, 0.005, 0.01]
  simplex_parameterization:
    - dirichlet
required_diagnostics:
  dirichlet:
    - log_prob
    - entropy
    - alpha_min
    - alpha_max
    - alpha_mean
invalid_if_non_finite:
  - log_prob
  - entropy
